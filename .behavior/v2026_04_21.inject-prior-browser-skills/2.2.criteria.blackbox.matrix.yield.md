# blackbox criteria coverage matrix

## matrix.1 — browser.start

| ind: session state | ind: flags | dep: browser | dep: endpoint | dep: output |
|--------------------|------------|--------------|---------------|-------------|
| no browser active | (default) | launches with stealth | writes ws-endpoint | success |
| no browser active | --headless | launches headless with stealth | writes ws-endpoint | success |
| no browser active | --no-stealth | launches without stealth | writes ws-endpoint | success |
| browser already active | (any) | no change | extant | "already active" |

coverage: complete — all flag combinations covered.

## matrix.2 — browser.stop

| ind: session state | dep: browser | dep: endpoint | dep: output |
|--------------------|--------------|---------------|-------------|
| browser active | terminates | removed | success |
| no browser active | n/a | n/a | "no browser active" |

coverage: complete.

## matrix.3 — browser.describe

| ind: session state | dep: output |
|--------------------|-------------|
| browser active with tabs | lists tabs (index, url, title, focused) |
| no browser active | error: no browser found |

coverage: complete.

## matrix.4 — browser.action

| ind: browser state | ind: playbook | ind: --tab | dep: execution | dep: output |
|--------------------|---------------|------------|----------------|-------------|
| active | exists | (default: 0) | runs on tab 0 | url before/after, result |
| active | exists | N (valid) | runs on tab N | url before/after, result |
| active | exists | N (invalid) | fails | error: tab not found |
| active | not found | (any) | fails | error: playbook not found |
| not active | (any) | (any) | fails | error: no browser |

coverage: complete.

## matrix.5 — browser.snapshot

| ind: browser state | ind: target mode | ind: url match | dep: artifacts | dep: output |
|--------------------|------------------|----------------|----------------|-------------|
| active | --focused | n/a | all captured | success |
| active | --tab N --url "x" | matches | all captured | success |
| active | --tab N --url "x" | mismatches | none | error: URL mismatch |
| active | --tab N (no --url) | n/a | none | error: --url required |
| active | subcommand (e.g. screen) --focused | n/a | single artifact | success |
| not active | (any) | n/a | none | error: no browser |

coverage: complete.

## matrix.6 — briefs (what mechanic learns)

| ind: brief category | ind: usecase | dep: knowledge gained |
|---------------------|--------------|----------------------|
| stealth | cloudflare detection | 3-tier escalation, detection signals |
| auth | cross-session | storageState persist/restore, respawn for mode switch |
| cache | expensive scrapes | withRemoteStateCache wraps queries, mutations invalidate |
| diagnosis | test failure | snapshot before hypothesis, grep HTML for data-testid |

coverage: complete — all four brief categories covered.

## matrix.7 — enrollment

| ind: mechanic state | dep: briefs | dep: skills |
|---------------------|-------------|-------------|
| needs browser automation | stealth, auth, cache, diagnosis briefs available | browser.* skills available |

coverage: minimal — single happy path. appropriate for enrollment (binary outcome).

---

## gaps identified

none — all matrices show complete coverage for the defined usecases.

## decomposition notes

no decomposition needed — all matrices have 2-3 independent dimensions, well within the 4-dimension threshold. the behavioral boundaries are appropriately scoped.
