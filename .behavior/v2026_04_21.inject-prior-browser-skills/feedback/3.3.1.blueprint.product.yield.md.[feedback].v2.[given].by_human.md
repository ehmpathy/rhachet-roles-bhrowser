emit your response to the feedback into
- .behavior/v2026_04_21.inject-prior-browser-skills/3.3.1.blueprint.product.yield.md.[feedback].v2.[taken].by_robot.md

1. emit your response checklist
2. exec your response plan
3. emit your response checkoffs into the checklist

---

first, bootup your mechanics briefs again

npx rhachet roles boot --repo ehmpathy --role mechanic

---
---
---


# blocker.1


#### blocker.2 — missing brief: CDP connection patterns

**research (3.1.3) explicitly marks for creation:**
```
### ref.cdp-reconnection-patterns.md [CREATE]
**content**: ws-endpoint persistence, keepalive pings, connection recovery
```

**blueprint filediff tree omits this entirely.**

**research findings that need teaching:**
- CDP connections die silently after 5-6 hours idle
- chrome m136+ requires `--user-data-dir` for remote debug
- keepalive pings (`Runtime.evaluate`) every 2-5 minutes required
- recovery patterns when connection drops

**impact:** users will hit silent session death with no documented recovery path. the `browser_keepalive()` function exists in lib but the "why" isn't taught.

**fix:** add to reliability or diagnosis:
- `reliability/ref.cdp-connection-patterns.md`


---

---

#### nitpick.2 — flag inconsistency: --stealth pattern

**matrix shows:** `--no-stealth` (negation pattern)
**feedback says:** `--stealth enabled|disabled` (explicit value)

**inconsistency:** `--mode HEADLESS|HEADFUL` uses enum, but `--no-stealth` uses negation flag.

**fix:** align to explicit value pattern per feedback:
```
--stealth enabled   (default if omitted)
--stealth disabled
```

update criteria, matrix, and codepath tree.

---

