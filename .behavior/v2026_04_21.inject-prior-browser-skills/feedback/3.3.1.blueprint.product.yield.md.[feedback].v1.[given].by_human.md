emit your response to the feedback into
- .behavior/v2026_04_21.inject-prior-browser-skills/3.3.1.blueprint.product.yield.md.[feedback].v1.[taken].by_robot.md

1. emit your response checklist
2. exec your response plan
3. emit your response checkoffs into the checklist

---

first, bootup your mechanics briefs again

npx rhachet roles boot --repo ehmpathy --role mechanic

---
---
---


# blocker.1


    ### blocker: missing brief from research

    research (3.1.3) explicitly marks for creation:
    ```
    ### ref.cdp-reconnection-patterns.md [CREATE]
    **content**: ws-endpoint persistence, keepalive pings, connection recovery
    ```

    **but the blueprint filediff tree omits this brief entirely.**

    research findings that need to be taught:
    - CDP connections "significantly lower fidelity" than playwright protocol
    - silent death after 5-6 hours idle
    - chrome m136+ requires `--user-data-dir` for remote debug
    - keepalive pings (`Runtime.evaluate`) every 2-5 minutes required

    without this brief, users will hit silent session death with no documented recovery path.

---

# blocker.2


### blocker: respawn/mode-switch workflow unaddressed

**wish explicitly states:**
> "its critical that folks always have the capacity to, as a human, answer captchas and then handoff that same browser session to a headless browser"

**vision describes:**
> "respawn for mode switch (headless ↔ headful)"
> "handle.respawn({ mode: 'HEADFUL' }) — save session, close browser, relaunch in new mode, restore session"

**criteria blackbox only has:**
```
given('--mode HEADFUL')
  when('browser.start --mode HEADFUL')
    then('launches in headful mode for human interaction')
```

**gap:** no criteria for:
- switching modes on an active session
- preserving storageState during mode switch  
- the respawn workflow that enables captcha handoff

this is a core wish requirement with zero coverage.


---


### blocker: storageState persist/restore not skill-exposed

**vision says:**
> "auth (if needed) — playbook logs in, writes storageState"
> "next session — reconnects via ws-endpoint, restores storageState"

**current state:** auth pattern documented via brief, but no skill like `browser.auth.save` or `browser.auth.restore`.

**problem:** users must read the brief, understand the pattern, and implement it in playbooks. this violates pit-of-success — the wish says "critical" but the blueprint relies on documentation rather than skill enforcement.

---

### divergence: --stealth flag in matrix but not criteria

**criteria matrix (2.2) shows:**
```
| no browser active | --no-stealth | launches without stealth | writes ws-endpoint | success |
```

**criteria blackbox (2.1) only has:**
```
given('--mode HEADLESS')
  when('browser.start --mode HEADLESS')
    then('launches in headless mode')
```

the `--stealth` / `--no-stealth` flags appear in matrix but are absent from blackbox criteria. this is a specification divergence — the contract is unclear.


also, should use --stealth enabled|disabled , where --stealth by default is enabled if not explicitly said enabled|disabled
