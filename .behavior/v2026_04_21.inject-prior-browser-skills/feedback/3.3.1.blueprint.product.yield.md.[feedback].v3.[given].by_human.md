emit your response to the feedback into
- .behavior/v2026_04_21.inject-prior-browser-skills/3.3.1.blueprint.product.yield.md.[feedback].v3.[taken].by_robot.md

1. emit your response checklist
2. exec your response plan
3. emit your response checkoffs into the checklist

---

first, bootup your mechanics briefs again

npx rhachet roles boot --repo ehmpathy --role mechanic

---
---
---


# blocker.1


### blocker.2 — test coverage gap for new skill

**issue:** `browser.session.sh` is a NEW skill (not cloned from declastruct-squarespace) but has zero test cases in the test tree.

**evidence:**
- blueprint line 13: `browser.session.sh [+]`
- test coverage table (lines 350-358): no entry for browser.session
- test tree (lines 391-408): no browser.session.integration.test.ts

**hazard:** new code without tests = regression risk + no verification of criteria.

**fix:** add to test tree:
```
├── [+] browser.session.integration.test.ts
```

add test cases:
| skill | positive | negative | edge |
|-------|----------|----------|------|
| browser.session get | 1 | 1 | 0 |
| browser.session set --from path | 1 | 1 | 0 |
| browser.session set --from @storage | 1 | 1 | 0 |
| browser.session del | 1 | 1 | 0 |

---



## behavior hazard prevention

### nitpick.4 — stealth default inconsistency in codepath tree

**issue:** codepath tree shows:
```
├── parse_args()  [+] --session, --mode, --stealth enabled|disabled
```

but interface alignment section shows:
```
| `--stealth` | `enabled` (default), `disabled` | browser.start.sh |
```

**clarification needed:** is default `enabled` if flag omitted, or must it be explicit?

**recommendation:** add to codepath tree:
```
├── parse_args()  [+] --session, --mode, --stealth (default: enabled)
```


must be made explicit, yes


---



### nitpick.5 — missing error hint for browser.session

**issue:** error output format table (lines 483-488) doesn't include browser.session error hints.

**fix:** add:
| error | hint |
|-------|------|
| storage state not found | "no session state found for 'X'. run browser.session set --from @storage first" |
| source file not found | "storage file 'X' not found at path Y" |

---

