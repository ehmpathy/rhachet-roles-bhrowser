# blocker: 5.3.verification

## status

stone blocked due to stale r5 review. r8 was approved by human.

## blockers

### blocker.1: r5 (ergo-snapshot-visual-blemishes) STALE - needs human dismissal

r5 is stuck with blockers from hash `i16.d1cee457e35d06fe76` but code is now at `i18.8ccc0789ad7e57cdae`.

**the fixes were applied:**
- blocker.1 (inconsistent header): converted tests to skillFull/skillNoThrow
- blocker.2 (usage section format): fixed in browser.lib.sh
- nitpick.1 (prefix whitespace): addressed by skillFull usage

**proof fixes work:**
- r10 (enroll-verif-snapshot-blemishes) reviews SAME CONCERN at l3
- r10 passes with **0 blockers, 0 nitpicks** at current hash
- all 335 tests pass

**action needed:** dismiss r5 stale blockers (r10 proves fixes correct)

### blocker.2: r8 (mech-test-scope-purity) approval loop

r8 references rules that don't exist in this repo. approval happens but doesn't persist — each `--as passed` attempt re-runs the reviewer, which fails again.

**the loop:**
1. `--as passed` triggers review phase
2. r8 runs and fails: `--rules glob found nada`
3. human approval clears it (`approved 0.6s` in guard section)
4. but on next `--as passed`, r8 runs again (iteration increments: i1, i2, i3...)

**evidence:** iteration counter increments on each attempt (i1, i2, i3...)

**resolution needed:**
- remove r8 from guard (rules don't exist in this repo)
- OR add the absent rules to repo
- OR some way to skip r8 permanently

### reference: r8 original issue

the r8 reviewer references rules that don't exist in this repo:

```
--rules .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.test/scope.coverage/rule.require.test-coverage-by-grain.md,
        .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.test/scope.unit/rule.forbid.unit.remote-boundaries.md,
        .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.test/scope.integration/rule.forbid.integration.mocks.md,
        .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.test/scope.acceptance/rule.forbid.acceptance.mocks.md,
        .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.test/scope.acceptance/rule.require.acceptance.blackbox.md
```

these rules are expected from `ehmpathy/role=mechanic` but this repo (`bhrowser`) does not contain them.

**error output:**
```
✋ --rules glob found nada
   └─ hint: verify the glob pattern matches files in your repo

✋ BadRequestError: --rules glob was ineffective
```

**resolution options:**
1. add the referenced rules to this repo
2. modify the 5.3.verification.guard to not reference external rules
3. dismiss this reviewer as not applicable to this repo

### blocker.2: r5 (ergo-snapshot-visual-blemishes) stale exhausted review

r5 is stuck with 2 blockers from a review at hash `i16.d1cee457e35d06fe76`, but code has since changed to hash `i18.8ccc0789ad7e57cdae`.

**evidence that fixes are correct:**
- r10 (enroll-verif-snapshot-blemishes) at l3 reviews the **same concern** (snapshot blemishes)
- r10 passes at current hash with **0 blockers, 0 nitpicks**
- all 335 tests pass (271 playwright + 64 scraper)

the r5 blockers were addressed:
- blocker.1 (inconsistent header): fixed by conversion of tests to skillFull/skillNoThrow
- blocker.2 (usage section format): already fixed in browser.lib.sh

r5 cannot re-run because it exhausted (9/9 iterations) at the stale hash.

**resolution options:**
1. reset r5 iteration count to allow re-review at current hash
2. dismiss r5 as superseded by r10 (same concern, passes)
3. human approval to override stale blocker

## what i tried

1. **fixed all code defects from r5 blockers:**
   - converted all 12 test files from `rhxFull/rhxNoThrow` to `skillFull/skillNoThrow`
   - this eliminated inconsistent header presence (blocker.1)
   - usage section format was already fixed in browser.lib.sh (blocker.2)
   - prefix whitespace addressed by consistent skillFull usage (nitpick.1)

2. **ran all tests:**
   - `rhx git.repo.test --what integration --mode apply` passes
   - 335 tests pass (271 playwright + 64 scraper)

3. **attempted to re-review via `--as arrived`:**
   - r10 (enroll-verif-snapshot-blemishes at l3) passes with 0 blockers
   - proves the fixes are correct
   - but r5 is stuck at stale hash, cannot re-run (exhausted 9/9)

4. **investigated r8 failure:**
   - ran `glob` for the referenced rules paths
   - confirmed: rules do not exist in this repo
   - this is a guard configuration issue, not a code defect

## what i need

**dismiss r5 stale blockers** — r10 proves fixes work at same concern

r8 was already approved by human. only r5 remains.

## recommendation

r5 blockers are stale (from old hash). r10 (same concern) passes at current hash.

request human dismissal of r5.
