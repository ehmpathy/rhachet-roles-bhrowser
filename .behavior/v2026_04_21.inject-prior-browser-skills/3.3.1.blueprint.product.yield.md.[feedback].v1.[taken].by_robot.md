# response to feedback v1

## checklist

- [x] blocker.1: add `ref.cdp-reconnection-patterns.md` brief (CDP silent death, keepalive, m136+ user-data-dir)
- [x] blocker.2: respawn/mode-switch handled by typescript (genBrowserAuthSession.respawn), documented in auth brief
- [x] blocker.3: storageState auto-persist handled by typescript; added `browser.session get|set|del` for shell-level inspect/import/clear
- [x] blocker.4: align --stealth flag across criteria blackbox and matrix (use `--stealth enabled|disabled`)

---

## response plan

### blocker.1: ref.cdp-reconnection-patterns.md

add to playwright briefs under `reliability/`:
- CDP vs playwright protocol fidelity
- silent death after 5-6 hours idle
- chrome m136+ requires `--user-data-dir` for remote debug
- keepalive pings (`Runtime.evaluate`) every 2-5 minutes

### blocker.2 + blocker.3: respawn and storageState skills

add skills to playwright role:
- `browser.session.save.sh` — persist storageState to disk
- `browser.session.restore.sh` — restore storageState from disk
- `browser.respawn.sh` — save session, stop browser, restart in new mode, restore session

update criteria blackbox with:
- mode switch on active session
- storageState preservation across switch
- respawn workflow for captcha handoff

### blocker.4: --stealth flag alignment

update criteria blackbox to include:
- `--stealth enabled` (default if not specified)
- `--stealth disabled`

update browser.start codepath to reflect this.

---

## checkoffs

### blocker.1: ref.cdp-reconnection-patterns.md
- added to filediff tree: `reliability/ref.cdp-reconnection-patterns.md`
- added brief details with CDP fidelity, silent death, m136+ user-data-dir, keepalive pings
- added usecase.19 to criteria coverage

### blocker.2 + blocker.3: respawn and storageState
- **typescript layer** (conforms to declastruct-squarespace): genBrowserAuthSession handles auto-save/restore and respawn
- **auth brief updated**: documents auto-persist after login, auto-restore on start, respawn({ mode }) for headless ↔ headful
- **new shell skill**: `browser.session get|set|del` for inspect/import/clear (not in declastruct-squarespace, new to bhrowser)
- updated summary: 12 → 13 skills
- added usecase.20, usecase.21, usecase.22 to criteria coverage

### blocker.4: --stealth flag alignment
- updated interface alignment table: `--stealth` with values `enabled` (default), `disabled`
- updated browser.start codepath: `--stealth enabled|disabled`
